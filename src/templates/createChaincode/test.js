const serializeArguments = require('../common/convertors/serializeArguments');
const ChaincodeStub = require('./mocks/ChaincodeStub');

describe('YOUR_CHAINCODE_NAME', () => {
    
    test('', async () => {
        
    });

});